import React from 'react';

export default function Home() {
  return (
    <div>
      <p>
        This is an page app built with React and
        React&nbsp;Router using avigate with the links below and refresh the
        page or copy/paste the url to test out the redirect functionality
        deployed to overcome GitHub&nbsp;Pages incompatibility with single page
        apps (like this one).
      </p>
    </div>
  );
}
